import { Box } from '@mui/material';
import React from 'react';

function ProductDetailsDesktop({ product, relevantProducts, seenProducts }) {
    return (
        <Box display={{ xs: "none", md: "block" }}>
            desktop
        </Box>
    );
}

export default ProductDetailsDesktop;